{
  "name": "voice-selfcare-agent",
  "version": "1.0.0",
  "description": "AI-powered voice health advisor — triage, guidance, and facility referrals via phone",
  "type": "module",
  "main": "src/cloudflare-worker.js",
  "scripts": {
    "test": "node --test --test-concurrency=1",
    "start": "wrangler dev",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:cloudflare": "wrangler deploy",
    "cf:whoami": "wrangler whoami"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
